# Install Prisma-related packages nested inside this workspace (not hoisted to root).
# Prisma CLI v7 resolves @prisma/client relative to its own install location,
# so it must live in apps/nestjs/node_modules rather than the monorepo root.
install-strategy=nested
